import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'feu.pharmacie.ionic',
  appName: 'feu-pharmacie-ionic',
  webDir: 'dist'
};

export default config;
